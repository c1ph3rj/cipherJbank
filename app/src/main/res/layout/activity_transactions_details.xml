<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/gradient1"
    android:layout_height="match_parent"
    tools:context=".TransactionsDetails">

    <RelativeLayout
        android:id="@+id/scrollViewForUserList"
        android:layout_width="320dp"
        android:layout_height="500dp"
        android:background="@drawable/user_login_layout_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.494"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <RelativeLayout
            android:id="@+id/ListLayout"
            android:layout_width="320dp"
            android:layout_height="400dp"
            android:layout_above="@+id/button2"
            android:layout_alignParentStart="true"
            android:layout_marginStart="-1dp"
            android:layout_marginBottom="25dp"
            tools:layout_editor_absoluteX="91dp">
            <ListView
                android:id="@+id/list_of_users"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scrollbars="none" />
        </RelativeLayout>

        <Button
            android:id="@+id/button2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginEnd="113dp"
            android:textColor="@color/white"
            app:backgroundTint="@color/Blue"
            app:cornerRadius="8dp"
            android:onClick="onClickBackOnTransaction"
            android:layout_marginBottom="19dp"
            android:text="@string/back_on_transaction" />
    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>